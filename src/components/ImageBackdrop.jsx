import { styled } from "@mui/material/styles";

export const ImageBackdrop = styled("span")(() => ({
  position: "absolute",
  left: 0,
  right: 0,
  top: 0,
  bottom: 0,
  backgroundColor: "black",
  opacity: 0.7,
}));

export const IntroPageBackdrop = styled("span")(() => ({
  position: "absolute",
  left: 0,
  right: 0,
  top: 0,
  bottom: 0,
  backgroundColor: "black",
  opacity: 0.4,
}));
